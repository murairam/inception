[mysqld]
skip-networking=0
# bind-address=0.0.0.0 is required for Docker inter-container communication
# Security is provided by Docker network isolation (port not exposed to host)
bind-address=0.0.0.0
port=3306
socket=/run/mysqld/mysqld.sock

[client]
port=3306
socket=/run/mysqld/mysqld.sock
